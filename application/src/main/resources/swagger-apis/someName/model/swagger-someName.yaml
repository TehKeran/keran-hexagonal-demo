components:
  schemas:
    someNameApiObject:
      type: "object"
      properties:
        id:
          type: "string"
          format: "uuid"
        # new fields go here:

        lastUpdate:
          type: "string"
          format: "date"
        modifiedBy:
          type: "string"
